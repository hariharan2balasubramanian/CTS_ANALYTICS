version: 2

models:
  - name: stg_customer
    config: 
      schema: "{{env_var('DBT_STAGESCHEMA','STAGING_DEV')}}"

  - name: stg_orders
    config: 
      schema: "{{env_var('DBT_STAGESCHEMA','STAGING_DEV')}}"
  
